<%= simple_form_for([@city, @city.restaurants.build]) do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :image %>
    <%= f.button :submit, :class => "btn btn-primary" %>
<% end %>