<%= simple_form_for([@city, @city.citywalks.build]) do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :duration %>
    <%= f.input :place %>
    <%= f.input :image %>
    <%= f.button :submit, :class => "btn btn-primary" %>
<% end %>