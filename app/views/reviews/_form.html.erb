<%= simple_form_for [reviewable.city.country, reviewable.city, reviewable,  Review.new] do |f| %>
    <%= f.input :body, placeholder: "Add a review", label: false %>
    <%= f.button :submit, :class => "btn btn-primary" %>
<% end %>