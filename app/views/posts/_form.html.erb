<%= simple_form_for @post, :html => { :multipart => true } do |f| %>
    <%= f.input :title, label: "Title Name" %>
    <%= f.input :content %>
    <%= f.button :submit, :class =>"btn btn-primary" %>
<% end %> 